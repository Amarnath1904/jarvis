/**
 * Utility functions for the renderer process
 */

/**
 * Show alert message
 * @param {string} message - Message to display
 */
export function showAlert(message = 'Hello from JARVIS! 🚀') {
    alert(message);
}

